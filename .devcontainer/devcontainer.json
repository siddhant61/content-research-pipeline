{
  "name": "Content Research Pipeline Codespace",
  "build": {
    "dockerfile": "Dockerfile",
    "context": "."
  },
  "features": {
    "ghcr.io/devcontainers/features/node": {
      "version": "lts"
    },
    "ghcr.io/devcontainers/features/python": {
      "version": "3.11"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "esbenp.prettier-vscode",
        "ms-azuretools.vscode-docker",
        "eamodio.gitlens"
      ]
    }
  },
  "postCreateCommand": "python -m pip install --upgrade pip || true; if [ -f requirements.txt ]; then pip install -r requirements.txt || true; fi; if [ -f pyproject.toml ]; then python -m pip install -U poetry || true; fi; if [ -f .devcontainer/postCreate.sh ]; then bash .devcontainer/postCreate.sh || true; fi",
  "forwardPorts": [8000, 8080],
  "remoteUser": "vscode"
}